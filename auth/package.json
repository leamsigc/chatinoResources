{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.4.0",
    "prisma": "^4.8.1",
    "@prisma/client": "^4.8.1",
    "@lucia-auth/adapter-prisma": "^1.0.0",
    "lucia-auth": "^1.0.0"
  },
  "dependencies": {
  }
}
